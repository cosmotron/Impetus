{% macro table(title, path_name) %}
    <h2>{{ title }}</h2>
    <p>
        <a href="{{ path(path_name) }}">View as HTML</a>
        &mdash;
        <a href="{{ path(path_name, { '_format': 'csv' }) }}">Download CSV</a>
    </p>
{% endmacro %}